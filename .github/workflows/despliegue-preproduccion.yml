name: Despliegue preproducción
on:   
  push:
    branches:
      - main

jobs:
  despliegue-pre:
    name: Despliegue preproducción
    env:
      SF_ACCESS_TOKEN: ${{ secrets.SALESFORCE_TOKEN }} 
      SF_INSTANCE_URL: https://uoctfmciberseguridad3-dev-ed.develop.my.salesforce.com
      SF_INSTANCE_ALIAS: desarrollo
    runs-on: ubuntu-latest
    steps:
      - name: Run despliegue a preproducción
        uses: TFM-UOC-JMGS-Ciberseguridad/demo-repository/docker-actions/despliegue-preproduccion@main
