name: Despliegue preproducción
on:   
  push:
    branches:
      - main

jobs:
  despliegue-pre:
    name: Despliegue preproducción
    runs-on: ubuntu-latest
    steps:
      - name: Run despliegue a preproducción
        uses: TFM-UOC-JMGS-Ciberseguridad/demo-repository@latest
