on: [push]

jobs:
  salesforce-job:
    runs-on: ubuntu-latest
    name: Deployment and test job
    steps:
      - name: Acción de Despliegue
        id: despliegue
        uses: atmintfmjmgs/tfm-image01@v2
      #  with:
      #    who-to-greet: 'Mona the Octocat'
